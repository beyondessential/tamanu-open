// Commented out because we need to upgrade typescript!
export type DateString = string; //`${number}-${number}-${number}`;
export type DateTimeString = string; //`${number}-${number}-${number} ${number}:${number}:${number}`
